name: AdaptiveGEMM CI

on:
  push:
    branches:
      - public-release
      - main
      - phase28-perf-tuning
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build and precision test (container)
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:12.6.2-devel-ubuntu22.04
    steps:
      - uses: actions/checkout@v4
      - name: Install build tools
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y cmake ninja-build g++ git python3-dev python3-pip python3-venv
          python3 -m venv venv
          ./venv/bin/pip install pytest numpy
      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DPython_EXECUTABLE=$PWD/venv/bin/python
      - name: Build
        run: cmake --build build --target adaptive_gemm_demo adaptive_gemm_precision_test adaptive_gemm_py

  gpu-validate:
    name: GPU precision test (self-hosted)
    needs: build
    runs-on: [self-hosted, linux, gpu]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python dependencies
        run: pip3 install pytest numpy --break-system-packages
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build
      - name: Precision test
        run: ctest --test-dir build -C Release --output-on-failure
